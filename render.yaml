services:
  - type: web
    name: turkey-pulse
    runtime: static
    branch: main
    buildCommand: echo "Turkey Pulse static PWA - no build step"
    staticPublishPath: ./web
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - web/**
      ignoredPaths: []
